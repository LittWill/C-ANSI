#include <stdio.h>
#include <locale.h>
#define par(n) ((n) % 2 == 0)
int main (void){
setlocale("LC_ALL", "Portuguese");
    int a;
    printf("Insira um número: ");
    scanf("%d", &a);
    puts(par(a) ? "O número é par." : "O número é ímpar.");
    return 0;

}
